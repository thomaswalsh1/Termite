export { addLog, clearHistory, createTerminal, execute, setInput, setCwd } from "./functions";

export type {
  TerminalEntry,
  TerminalState,
  TerminalHandler,
  CreateTerminalOptions,
} from "./types";
